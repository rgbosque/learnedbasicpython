> mkdir .ssh | this will create directory
> ssh-keygen.exe | this will generate key
> car /c/User/<your path>/.ssh/id_rsa.pub | just to check if you have a key 
> then copy the key and login to github
> go to settings and select SSH and GPG keys
> add keys

* steps to initialized Git
> git config --global user.name "Rodel Bosque"
> git config --global user.email "rgbosque@gmail.com"
> git config --global push.default matching
> git config --global alias.co checkout
> git init

* setup github
> create repository for the code
> copy the code from github something like this and past to terminal
git remote and origin https://github.com/.../learnedbasicpython.git


* once initialized
> git add . (add all code to git)
> git commit -m "Initial commit"
> git push -u origin master